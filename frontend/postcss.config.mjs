// Location: frontend/postcss.config.mjs
// Purpose: PostCSS configuration file that defines CSS processing plugins
// This file configures Tailwind CSS and Autoprefixer for optimal CSS processing

/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;
