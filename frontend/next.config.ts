// Location: frontend/next.config.ts
// Purpose: Next.js configuration file that defines build-time and runtime settings
// This file controls routing, optimization, environment variables, and other Next.js features

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
